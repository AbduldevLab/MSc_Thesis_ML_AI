% this file is called up by thesis.tex
% content in this file will be fed into the main document

\chapter{Conclusion}\label{ch:chapter6}


% ----------------------- paths to graphics ------------------------

% change according to folder and file names
\ifpdf
    \graphicspath{{6_conclusions/figures/PNG/}{6_conclusions/figures/PDF/}{6_conclusions/figures/}}
\else
    \graphicspath{{6_conclusions/figures/EPS/}{6_conclusions/figures/}}
\fi


% ----------------------- contents from here ------------------------


\section{Summary}
This thesis developed and evaluated a longitudinal \gls{ML} pipeline for predicting chronic disease onset in older adults across all five waves of \gls{TILDA} data. Manual variable screening reduced over 5,000 raw variables to a structured, domain organised dataset. A deterministic cleaning pipeline was applied and validated against published benchmarks. Four model families were evaluated across three modelling designs and 24 disease specific outcomes. Pooled multi-wave designs substantially outperformed the single wave baseline, predictive signal concentrated in a compact cluster of health burden indicators, and no domain specific add-on feature contributed statistically significant independent predictive value. Code and pipeline scripts are available via the repository referenced in Section~\ref{sc:contributions}.

% ---------------------------------------------------------------------------
\section{Conclusions}
Longitudinal pooling works, and restricting to the core health burden predictor set adds no statistical significance in performance. The gains documented in Chapter~\ref{ch:chapter5} reflect a structural alignment between predictor horizon and outcome definition. Predicting clinically coded disease eight years ahead is a harder task than predicting self-reported health burden over two-year transitions, and matching these two layers is what drives the improvement. Chapter~\ref{ch:chapter2} framed prediction as estimating $P(\text{disease} \mid X)$, where the marginal gain from any added feature depends on how much independent information it carries. The domain counts confirm this. Of the $3{,}113$ retained variables (Table~\ref{tab:feature_groups}), sociodemographic context ($844$), psychological and cognitive measures ($291$), and nutritional biomarkers ($22$) produced no top-20 predictors. Lifestyle behaviours ($120$ variables) contributed one item, flu vaccination, which functioned here as a proxy for healthcare engagement rather than a modifiable behaviour. Signal was concentrated in physical health ($853$ variables) and functional status ($174$ variables), and just 20 of those $1{,}027$ sufficed. Adding $1{,}277$ variables across the remaining four domains, each genuinely relevant to ageing, did not move the posterior.

The validation in Chapter~\ref{ch:chapter4} adds a dimension often missing from published work. Encoding decisions can materially affect model performance in health survey data. Recoding six frailty index items to respect published ordinal deficit scoring improved \gls{AUC} from $0.591$ to $0.636$ under the biologically informed proxy, a $7.6\%$ gain attributable entirely to encoding with no change in the feature set or model.

% ToDo: maybe reword 'natural' inside Three designs form a natural experiment.
Three designs form a natural experiment. Adding temporal structure (D1 to D2) improved performance substantially. Restricting features to 20 (D2 to D3) produced no further loss, confirming that the signal is compact and that the cross-design gap reflects longitudinal structure, not feature count. Hyperparameter optimisation in D3 similarly produced negligible gains.

Track~B reinforces the pattern. Outcomes with sufficient incidence and consistent wave measurement generalised well, those depending on sparse events or indirect proxy targets did not, regardless of algorithm. Prediction is bounded by the depth of the data, not the complexity of the model. Significant gains require the linked records held under elevated access, as outlined in Section~\ref{sc:future_work}.

\section{Data availability} 
This analysis was conducted using the publicly accessible \gls{TILDA} release (waves~1--5), a pseudonymised dataset covering self-reported health, physical, cognitive, nutritional, and behavioural variables, together with clinical coded disease indicators from wave~2 onwards. The datasets are accessible through the \gls{ISSDA} at \url{https://tilda.tcd.ie/data/accessing-data/}.

Several data sources relevant to chronic disease prediction are not included in the public release. These require on-site access at \gls{TILDA}'s Trinity College Dublin facility or remote access through the \gls{TILDA} VISTA trusted research environment, and include a dedicated biomarkers dataset, the wave~3 \gls{MRI} dataset, an epigenetics dataset, linked primary care records, and linked mortality records. Access is subject to formal application to the \gls{TILDA} data access committee and operates on a cost recovery basis where institutional funding is available. Future work drawing on these sources would require updated ethics approval, \gls{GDPR} training certification and a signed data use agreement with \gls{TILDA}.

\section{Ethics statement}  
The \gls{TILDA} study was reviewed and approved by the Faculty of Health Sciences Research Ethics Committee at Trinity College Dublin, and all participants provided written informed consent to their data being used for research purposes. The secondary analysis conducted in this thesis was approved by the University of Limerick Science and Engineering Research Ethics Committee. All data used were publicly released, pseudonymised datasets accessed through \gls{ISSDA}. No direct participant contact or identifiable personal data were involved in this analysis.

% ---------------------------------------------------------------------------
\section{Contributions}
This research makes contributions at three levels. \textbf{Empirically}, it demonstrates that pooled longitudinal modelling substantially improves predictive performance and stability for chronic disease onset in the \gls{TILDA} cohort, and identifies a compact cluster of self-reported health burden indicators as the primary driver of that signal. Domain features outside this core set showed no statistically significant independent predictive value, a result consistent across all model families and designs. \textbf{Methodologically}, a transparent, reproducible preprocessing pipeline for aligning heterogeneous longitudinal health survey data is documented in full, addressing a gap rarely covered in published \gls{ML} literature. \textbf{Clinically}, the results provide a data driven basis for risk stratification using publicly available survey data, and identify the conditions under which more complex predictor sets or model families are unlikely to improve performance without access to richer linked data.

% ToDo: great para but maybe a slight amber flag here: 'predicting general chronic disease onset' we are tyrion to comapre in text a spefic disease wiht general ? but we did do sopecfic disease it would be nbetter to compare 1.1 such as diabaetes i beleieve which we did under family history so mention taht instead as it directly sligns wiht our closing senetnce about public survey featires and our previous mention of limited access to richer linked data.
The results in Table~\ref{tab:lr_table} provide a concrete benchmark. Test \gls{AUC} values in D2 and D3 ($0.71$--$0.72$) sit at the upper end of the $0.64$--$0.71$ range reported in comparable longitudinal prediction studies, including \gls{AUC} $\leq 0.69$ for falls prediction in \gls{TILDA}~\cite{donoghueUsingConditionalInference2023} and $0.64$--$0.71$ for longitudinal mental health disorder prediction~\cite{zhangMachineLearningModels2025}. The present results are comparable with, and in some cases above these benchmarks while targeting a broader outcome using only publicly accessible data. The single wave baseline (D1: $0.53$--$0.58$) confirms that the gain is driven by the pooled longitudinal design, not algorithm choice. All four model families cluster within $0.001$ \gls{AUC} in D3, which reinforces this. The reported benchmark of $0.854$~\cite{xuTailoredMachineLearning2023} reflects a different task. Predicting diabetes from 105 curated predictors drawn from over 40,000 variables via a stacked ensemble is not comparable to predicting general chronic disease onset from public survey features.

% ---------------------------------------------------------------------------
\section{Future work}\label{sc:future_work}
The recently released sixth wave~\cite{ISSDA/RWNE1A_2025} of \gls{TILDA} data (December 2025), together with the Intellectual Disability Supplement and the COVID-19~\cite{ISSDA/A8YGJ5_2025} sub study, would extend the longitudinal coverage and introduce health trajectory data spanning a pandemic period, likely altering self-reported burden profiles in meaningful ways. Cross cohort validation using the \gls{RAND} harmonised \gls{TILDA} dataset, which maps variables to international standards compatible with cohorts such as \gls{HRS} and \gls{SHARE}, would test how well the identified health burden cluster generalises beyond the Irish context. Finally, access to the restricted biomarker panels, primary care records, and linked mortality data held within \gls{TILDA}'s secure research environment would allow future work to test whether the signal limits identified here reflect the nature of chronic disease risk itself or the constraints of the publicly available dataset.

% Todo: we say 'longitudinal health survey data do not yet exist as a shared resource' but becareful this is correct but i wpuld add a slight additon by awknlwoding that in itself that would be a big chalalneg aswell, health data vary widely etc  lots of challaneges such as confidentlity challneges so maybe direct the additonal snetcne towards a curated guide on tranfomrting health data as helaht is very subjective having one standrard format can be diffuclt. thats the idea im trying to portray but again im looking for a slight extension awknlowedgement of the challenges in creating a shared resource, but also the value of a curated guide on how to transform health data for \gls{ML} applications.
The encoding challenges encountered in preparing the \gls{TILDA} datasets point to a wider gap in the \gls{ML} literature. Standardised preprocessing protocols and auditable encoding indices for longitudinal health survey data do not yet exist as a shared resource. Establishing reproducible, domain aware transformation workflows would reduce the risk that performance differences across studies reflect encoding choices rather than genuine signal, supporting more reliable cross study comparisons in health data science.


% ---------------------------------------------------------------------------
% ----------------------- end of thesis sub-document ------------------------
% ---------------------------------------------------------------------------