% this file is called up by thesis.tex
% content in this file will be fed into the main document

\chapter{Implementation Details} \label{ch:chapter4} % top level followed by section, subsection


% ----------------------- paths to graphics ------------------------

% change according to folder and file names
\ifpdf
    \graphicspath{{4_chapter4/figures/PNG/}{4_chapter4/figures/PDF/}{4_chapter4/figures/}}
\else
    \graphicspath{{4_chapter4/figures/EPS/}{4_chapter4/figures/}}
\fi


% ----------------------- contents from here ------------------------
Following manual variable filtering in (Chapter~\ref{ch:chapter3}), the reduced datasets contained only the variables of interest. To further understand the characteristics of these retained variables, the datasets were profiled using an exploratory visualiser (\texttt{.ipynb}) file implemented with \texttt{pandas}, \texttt{numpy}, \texttt{matplotlib}, and \texttt{seaborn}. This step examined data types, missingness patterns, unique value distributions, and representative sample values across all waves. The purpose of this profiling stage was not transformation but to expose structural inconsistencies and latent formatting issues that would undermine direct numerical conversion.

\begin{table}[htbp]
\centering
\caption{Input shape for cleaning pipeline (post manual variable filtering).}
\label{tab:dataset_shape}
\begin{tabular}{|c|c|c|}
\hline
\textbf{Dataset} & \textbf{Row (participants)} & \textbf{Column (features)} \\
\hline
Wave 1 & 8501 & 809 \\
Wave 2 & 7206 & 482 \\
Wave 3 & 6397 & 639 \\
Wave 4 & 5713 & 592 \\
Wave 5 & 4978 & 577 \\
Harmonised & 8501 & 453 \\
\hline
\end{tabular}
\end{table}

For example, in Wave 1, the visualiser detected 650 categorical columns, 150 (\texttt{float64}) columns, and 9 object columns, while the harmonised dataset contained 304 categorical columns, 148 \texttt{float32} columns, and several integer columns. Age distributions confirmed that the study population was restricted to older adults across waves, however, ages were bottom coded for participants younger than 50 years and top coded for those aged 65--85 years and above. Missing value summaries indicated substantial variation (e.g., mean missing values ranging from $4.7\%$ in Wave 4 to $35\%$ in the harmonised dataset, with some variables exceeding $77\%$ missing).

Despite this, the exploratory visualiser was limited because the raw data were not yet cleaned, many outputs were incorrect or nonsensical due to hybrid formats and inconsistent coding. For instance, inspection revealed that many variables across both the wave specific and harmonised datasets, while appearing numeric at first glance, in fact contained hybrid representations combining numeric prefixes with descriptive text (e.g., ``1.Rarely or none of the time (less than 1 day)'', ``4.All of the time (5--7 days)'', or prefixed labels such as ``0.not coupled'', ``1.coupled''). A simplistic string stripping approach would therefore produce incorrect numeric values (for example, reducing ``5--7 days'' to \texttt{5}) resulting in silently introducing semantic distortion. Similarly, numeric columns sometimes contained sentinel codes (e.g., -98, -99) or embedded symbols (`$\$$', `$cm$', `$\%$') that skewed visual summaries.

These insights, revealed through systematic profiling, guided the development of the subsequent cleaning pipeline, ensuring consistent numeric logic representations (e.g., midpoints for ranges, standardised sentinel handling).

\section{Cleaning Engine}
The core challenge in converting heterogeneous variable types across the six datasets (Table~\ref{tab:dataset_shape}) was achieving a single numerical standard suitable for downstream analysis. This required enforcing \emph{numerical homogeneity}, defined here as representing all retained features using a single numerical representation (\texttt{float64}) with a unified missing or non response sentinel ($\texttt{-1.0}$). The exploratory profiling across all waves showed that categorical variables were dominant overall ($2,916$ columns), while numeric variables were fragmented across multiple types, with \texttt{float64} being the most prevalent native numeric representation ($462$ columns), motivating its use as a common standard to ensure consistency and compatibility with downstream \gls{ML} models, while accommodating decimal values arising from range midpoints, frequency normalisation, and ordinal scaling that cannot be reliably represented using integer types. To address this, the solution was implemented as a two stage system. The first stage performs automated \emph{variable scenario detection}, identifying the structural form of each variable based on observed value patterns using the \texttt{detect\_column\_scenario\_mapping} function (Appendix~\ref{app:variable_mapping_code}). This stage includes additional diagnostic categories used during development to surface unhandled edge cases.

The second stage applies a deterministic \emph{six step numerical cleaning engine} (Appendix~\ref{app:cleaning_pipeline_code}). While nine scenario categories were initially defined during development, three served exclusively as validation fallbacks and were progressively eliminated as detection coverage improved, with a full scenario to step mapping counts provided in Appendix~\ref{tab:cleaning_tables}. The final pipeline therefore consists of six active transformation steps, applied sequentially and uniformly across all waves.

\subsection{Stage 1: Variable scenario mapping and validation}
Stage 1 focused on automatically identifying how each variable should be handled during cleaning. The automated detection function in Appendix~\ref{app:variable_mapping_code} was developed to classify variables based on their observed value structure, including the presence of sentinel codes, numeric annotations, ordinal labels, and mixed string numeric formats. While multiple intermediate detection categories were used during development, these served solely as internal routing logic. While all variables passed sequentially through the six step cleaning pipeline, most were fully resolved in the earlier steps (Steps 1--5). The final step (Step 6 in Appendix~\ref{app:step6_code}) was applied only to residual variables that could not be deterministically resolved by earlier transformations.

The additional detection categories (e.g., ``single value categorical'', ``special symbol mixed'', and ``unknown/complex'') were intentionally included as diagnostic fallbacks to surface unhandled edge cases due to the complexity and size of the data. As rules matured, variables initially defaulted to these fallback categories were progressively resolved into earlier, well defined scenarios, leaving none remaining for the final pipeline execution.

To ensure robustness, the classification output was immediately processed by a validation routine that generated a comprehensive dictionary of feature states before and after cleaning, including all representative sample values for each variable. This allowed direct inspection of how the same variable was represented across waves prior to cleaning, particularly in cases where identical semantic categories appeared in different orders or formats. By comparing these grouped samples before and after each cleaning step, classification errors and inconsistent mappings were identified and corrected prior to execution of subsequent transformations.

The dictionary generated inside the \texttt{show\_grouped\_vars\_for\_scenarios} function (Appendix~\ref{app:md_script}) was formatted to create an automatic friendly ``.md'' file for rapid, surface level verification, which honed the final mapping dictionary (an example can be found in Appendix~\ref{app:md_audit}) for all features across all waves. This process of using automated detection followed by visual, manual validation was essential for correcting errors in classification logic before the subsequent scenario transformations were executed in the pipeline.

\subsection{Stage 2: Deterministic six step cleaning sequence}
The nine input scenarios were processed sequentially by the \emph{six step cleaning engine}, resolving simple and unambiguous cases first to maximise the proportion of clean numerical features before tackling more complex structures, particularly censored and multi-class scenarios (Figure~\ref{fig:cleaning_flow}). This deterministic sequence applied identical transformation logic to each wave, with Step 6 deriving numerical representations directly from the observed category tokens at runtime rather than relying on predefined wave specific encodings (see Appendix~\ref{app:step6_code}).

This design reflects the empirical diversity of survey encodings, where a small subset of variables only reveal their semantic structure after simpler transformations have been applied.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    node distance=12mm,
    every node/.style={rectangle, rounded corners, draw=black, align=center, minimum width=5.2cm, minimum height=1.1cm}
]

\node (step1) {Step 1:\\Empty / Blank Handling};
\node (step2) [below=of step1] {Step 2:\\Sentinel Standardisation};
\node (step3) [below=of step2] {Step 3:\\Numeric Cleaning};
\node (step4) [below=of step3] {Step 4:\\Censored / Ordinal Mapping};
\node (step5) [below=of step4] {Step 5:\\Binary Classification};
\node (step6) [below=of step5] {Step 6:\\Multi-class / Mixed Parsing};

\draw[->, thick] (step1) -- (step2);
\draw[->, thick] (step2) -- (step3);
\draw[->, thick] (step3) -- (step4);
\draw[->, thick] (step4) -- (step5);
\draw[->, thick] (step5) -- (step6);

\end{tikzpicture}
\caption{Flowchart of the final six step cleaning pipeline applied to all \gls{TILDA} waves. See full transformation tables in Appendix~\ref{tab:step6_full} and \ref{app:cleaning_pipeline_code} for a detailed progression ``\texttt{.ipynb}'' script at each scenario cleaning step.}
\label{fig:cleaning_flow}
\end{figure}

The final cleaning pipeline consisted of six deterministic steps applied uniformly across all \gls{TILDA} waves, with increasingly complex transformations handled in later stages. Steps 4--6 required particular attention due to hybrid, ordinal, or multi-class formats. A summary of each step and examples is provided below.

\begin{enumerate}
    \item \textbf{Empty / Blank Values:} All missing entries, empty strings, and whitespace were converted to the standard non-response sentinel ($\texttt{-1.0}$).
    
    \item \textbf{Sentinel Values:} Wave specific non-response codes (e.g., $\texttt{-98}$, $\texttt{-99}$) and textual sentinels (e.g., \texttt{"dk"}, \texttt{"not applicable"}) were mapped to the unified ($\texttt{-1.0}$) placeholder.

    \item \textbf{Numeric Transformation:} Numeric like columns containing embedded units, symbols, or hybrid strings (e.g., $\texttt{50+}$, $\texttt{<10}$, $\texttt{1-2.5}$) were parsed using regex and converted to \texttt{float64}. For ranges, midpoints \&/or boundary values were used. For censored or annotated values, appropriate upper/lower bounds were applied. 

    \item \textbf{Censored / Ordinal Variables:} This step addressed variables representing ordinal scales, bracketed numeric ranges, or frequency expressions (e.g., ``2--3 days a week'', ``daily''). Transformations included:
    \begin{itemize}
        \item[-] Converting textual ordinal labels and Likert scale responses to numeric equivalents.  
        \item[-] Mapping calendar based frequency descriptors to numeric weekly equivalents.  
        \item[-] Manual verification for low count or unusual variables, supported by the Markdown audit outputs (Appendix~\ref{app:md_audit}), to ensure semantic meaning was preserved.  
    \end{itemize}

    \item \textbf{Binary Categorical Variables:} Two-level features (e.g., Yes/No, Male/Female) were dynamically encoded using mappings derived from the observed value set of each variable:
    \begin{itemize}
        \item[-] Semantic rules were applied e.g., (``no'', ``not'', ``female'' $\to 0.0$) and (``yes'', ``male'' $\to 1.0$).  
        \item[-] Count based detection verified that variables had exactly two non-missing unique values.  
        \item[-] Features with more than two sample values were also analysed for sentinel presence:
        \begin{itemize}
        \item[-] If a variable had three observed values and one was a sentinel, it was treated as binary.  
        \item[-] If a variable had four observed values and two were sentinels, it was treated as binary.  
        \end{itemize}
        \item[-] Sentinels were preserved as $\texttt{-1.0}$.  
        \item[-] This approach ensured order invariant encoding across waves, even when original labels or numeric prefixes differed.
        \item[-] \textit{Example (sentinel case):} Feature \texttt{fl002\_5} with pre transformed values \texttt{[`NOT Getting in or out of bed', -1.0, `Getting in or out of bed']} was encoded as \texttt{[0.0, -1.0, 1.0]}, demonstrating how a sentinel preserves binary semantics.
    \end{itemize}

    \item \textbf{Multi-class / Mixed Variables:} Variables with remaining multi-class structure, mixed types, or unresolved string numeric hybrids after earlier transformations were processed in the final step.

    Technical implementation details for this step are provided in Appendix~\ref{app:step6_code}:
    \begin{itemize}
        \item[-] Lexical parsing extracted numbers, ranges, units, percentages, and textual frequency descriptors into floats.  
        \item[-] Sentinel handling ensured all known non-response codes mapped to ($\texttt{-1.0}$).  
        \item[-] Original values were grouped and assigned numeric identifiers in first seen order, non numeric categorical labels were assigned integer codes above the highest observed numeric value to prevent collisions.  
        \item[-] Manual inspection using the Markdown audit file verified that semantic equivalence was maintained across waves.  
        \item[-] Step 6 was designed to be \emph{order invariant}, dynamically interpreting category values within each wave rather than relying on predefined lookup tables shared across waves. This preserved consistent numeric representation regardless of variable ordering or wave specific encodings.
        \begin{itemize}
        \item[-] \textit{Example:} Step 5 handled simple binary variables such as \texttt{CHRany\_pain} (any pain reported) with pre transformed values \texttt{[`pain', `No pain']} and numeric mapping \texttt{[1.0, 0.0]}. In contrast, Step 6 dynamically processed multi-class variables like \texttt{we114} (pre tax wage/salary) many of which contained over 20 distinct pre transformed categories (e.g., \texttt{-1.0, `200-299', `Less than 100', ..., `6,000-6,999'}) into consistent numeric representations (e.g., \texttt{-1.0, 249.5, 99, ..., 6499.5}). This illustrates why Step 6 required wave specific, dynamic parsing to maintain semantic equivalence across diverse observed values.
        \end{itemize}
    \end{itemize}
\end{enumerate}

\subsection{Cross wave semantic verification}
Given the absence of guaranteed category ordering across waves, preserving semantic equivalence during numerical encoding was essential to avoid introducing distortions into downstream \gls{ML} modelling. Pre and post transformation values for each group were exported to a readable Markdown (\texttt{.md}) file, as shown in Appendix~\ref{app:md_audit}. This representation confirmed that features of known importance for downstream predictive models were preserved and correctly encoded. For example, features such as \texttt{ph001} (self-rated general health), \texttt{ph009} (comparative self-rated health), and \texttt{bh006} (smoking frequency) correspond to items identified by \cite{moguilnerImportanceAgePrediction2021} as among the most influential predictors in mortality models by the \gls{FI}. Retaining these features from the manual filtering thresholds applied in (Chapter~\ref{ch:chapter3}) and encoding them consistently across waves ensured that the cleaned dataset maintains coverage and semantic integrity, preventing distortions that could potentially compromise model performance.

A key challenge in cleaning the five \gls{TILDA} waves was the lack of guaranteed ordering for categorical values of the same variable across different waves. Table~\ref{tab:wave_ordering_example} illustrates this for Wave 4 and Wave 5.

For example, gender (\texttt{sex}) variables appeared under different value encodings, with varied category orderings and numeric prefixes across waves. Values such as \texttt{[`Male', `Female']}, \texttt{[`Female', `Male']}, and prefixed forms like \texttt{[``1.male', `2.female']} were all mapped to a consistent numeric representation (\texttt{1.0} for male, \texttt{0.0} for female). This ensured that semantic equivalence, rather than positional ordering or wave specific encoding, determined numerical assignment.

The variable \texttt{ph001} found in Appendix~\ref{app:md_audit} corresponds to the survey question: “Now I would like to ask you some questions about your health. Would you say your health is…”?. Responses are ordinal (Poor to Excellent) with non-response categories. 

The grouped Markdown representation reveals three critical points: 
\begin{enumerate}
    \item Semantically identical categories are correctly grouped even under different variable names (\texttt{ph001} vs \texttt{ph009}), enabling direct comparison of representations that would otherwise appear fragmented across waves. 
    \item Numerical encoding is invariant to category ordering, preserving ordinal meaning across waves. Without this approach, positional encoding would assign inconsistent values, potentially distorting performance for downstream models.
    \item For variables with a large and heterogeneous set of observed values (e.g., income bands, frequency ranges, or mixed numeric text responses), the grouped view provided essential visibility into how complex category spaces were being collapsed into stable numeric representations, supporting both validation and error detection at scale.
\end{enumerate}

% ------------------------------------------------------------
% Side-by-side table: Wave 4 vs Wave 5 category order + positional codes
% ------------------------------------------------------------
\begin{table}[htbp]
\centering
\caption{Inconsistent category ordering for the same feature $ph009$ (\texttt{self-rated health compared to other people your age}) across waves and its impact on positional (order-based) label encoding.}
\label{tab:wave_ordering_example}
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.15}
% Total = 0.08 + 0.21 + 0.21 + 0.25 + 0.25 = 1.00\textwidth
\begin{tabular}{|C{0.1\textwidth}|C{0.20\textwidth}|C{0.20\textwidth}|C{0.20\textwidth}|C{0.20\textwidth}|}
\hline
\multirow{2}{*}{\textbf{Pos}} &
\multicolumn{2}{c|}{\textbf{Wave 4}} &
\multicolumn{2}{c|}{\textbf{Wave 5}} \\
\cline{2-5}
& \textbf{Category} & \textbf{Code} & \textbf{Category} & \textbf{Code} \\
\hline
1 & Good & 3.0 & Very Good & 4.0 \\
2 & Fair & 2.0 & Fair & 2.0 \\
3 & Poor & 1.0 & Excellent & 5.0 \\
4 & Excellent & 5.0 & Good & 3.0 \\
5 & DK & -1.0 & Poor & 1.0 \\
6 & Very Good & 4.0 & DK & -1.0 \\
7 & RF & -1.0 & RF & -1.0 \\
\hline
\end{tabular}
\end{table}

\section{Cleaning Validation}
The purpose of the cleaning validation stage was to assess whether the automated numerical parsing and encoding pipeline preserved meaningful biological and statistical structure within the cleaned datasets. Rather than asserting correctness based solely on successful execution, validation was performed by evaluating whether known relationships reported in the literature could be recovered using the cleaned data under realistic data governance constraints.

Due to restricted access to certain outcome variables within \gls{TILDA}, full end-to-end replication of all published models was not always feasible. In particular, access through the \texttt{Hotdesk Facility} for the official mortality outcomes is subject to additional governance requirements, including institutional \gls{GDPR} training certification and approval by the \gls{TILDA} data access committee \cite{HotDesksIrish}. Consequently, validation focused on two complementary strategies: (i) internal consistency checks against established biological and clinical relationships, and (ii) constrained replication of published modelling frameworks using available predictors and carefully documented proxy outcomes. Together, these approaches provide evidence that the cleaning pipeline produces numerically coherent datasets suitable for downstream statistical and machine learning analysis.

\subsection{Internal consistency and biological plausibility}
The first validation stage assessed whether cleaned predictors preserved biologically and clinically plausible relationships reported in prior \gls{TILDA} based studies. This analysis focused on predictors identified by \cite{davisLinearRegressionbasedMachine2021} as accounting for approximately $95\%$ of the explained variance in \gls{GSR}, defined as the difference between maximum and usual gait speed.

As \gls{GSR} is not directly available in the public Wave 3 dataset, validation was performed by examining the directional relationships between the seven primary predictors themselves. This approach evaluates whether the numerical encoding process, including sentinel handling and ordinal mapping, retained the expected internal structure of the data rather than reproducing outcome specific performance.

Figure~\ref{fig:heatmap} presents the correlation matrix for the selected predictors following cleaning. The observed relationships align with established findings in the literature. Grip strength exhibited a negative association with both age and chair stand time, reflecting the inverse relationship between muscular strength and physical performance decline. Cognitive performance (\gls{MOCA}) demonstrated a moderate negative correlation with age and a positive association with educational attainment, consistent with known patterns of cognitive reserve in older adults.

Table~\ref{tab:directional_validation} summarises the expected and observed directional relationships between key predictor pairs. In all examined cases, the direction and relative magnitude of the correlations were consistent with prior findings, supporting the conclusion that the cleaning pipeline preserved meaningful biological signals rather than introducing artefactual associations.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.8\textwidth]{heatmap.png}
\caption{Correlation heatmap for top 7 predictors (Wave 3).}
\label{fig:heatmap}
\end{figure}

\begin{table}[htbp]
\centering
\caption{Directional validation of the seven features explaining $\texttt{95\%}$ of \gls{GSR} variance as identified by \cite{davisLinearRegressionbasedMachine2021}.}
\label{tab:directional_validation}
\small
\begin{tabular}{|p{2.5cm}|p{2.5cm}|p{6.5cm}|c|}
\hline
\textbf{Predictor 1} & \textbf{Predictor 2} & \textbf{Expected Direction / Justification} & \textbf{Observed $r$} \\
\hline
\gls{GRIP} & Age & \textbf{Negative (-)}; \gls{GRIP} is a positive predictor of \gls{GSR}, while age is a negative predictor \cite{davisLinearRegressionbasedMachine2021}. & $-0.265$ \\
\hline
\gls{GRIP} & Chair Stands Time & \textbf{Negative (-)}; Higher strength (grip) is expected to correlate with faster (lower) performance time in physical tests \cite{davisLinearRegressionbasedMachine2021}. & $-0.170$ \\
\hline
\gls{MOCA} Score & Age & \textbf{Negative (-)}; Cognitive performance (\gls{MOCA}) contributes positively to \gls{GSR}, whereas increasing age is associated with reduced reserve \cite{davisLinearRegressionbasedMachine2021}. & $-0.381$ \\
\hline
Education & \gls{MOCA} Score & \textbf{Positive (+)}; Third level education and cognitive scores are both cited as positive contributors to cognitive reserve and \gls{GSR} \cite{davisLinearRegressionbasedMachine2021}. & $0.367$ \\
\hline
\gls{BMI} & Age & \textbf{Weak / Mixed}; \gls{BMI} and age both negatively impact \gls{GSR}, though their internal correlation in older cohorts is typically weak \cite{davisLinearRegressionbasedMachine2021}. & $-0.073$ \\
\hline
\end{tabular}
\end{table}

\subsection{Model based validation under governance constraints}

To further assess the suitability of the cleaned data for downstream modelling, a constrained replication of the methodology proposed by \cite{moguilnerImportanceAgePrediction2021} was undertaken. That study predicts 8-year mortality across Waves 1--5 using a \gls{FI} constructed from multiple health deficit variables, reporting \gls{AUC} values ranging from approximately $0.70$ (\gls{FI} only) to $0.80$--$0.90$ when combined with age.

Direct replication of the original mortality outcome was not possible due to governance restrictions on access to centrally linked mortality records maintained by Ireland’s Central Register Office. To address this limitation, a proxy mortality outcome was constructed following the descriptive statistics and cohort stratifications reported in the original study. Specifically, the proxy outcome preserved the reported sex distribution and overall event rate, while remaining independent of the predictors.

While this proxy outcome does not replicate true mortality status, it serves a diagnostic purpose: if the cleaned predictors are numerically coherent and semantically consistent, the resulting models should exhibit behaviour similar in structure, though not necessarily in magnitude, to those reported in the literature. In particular, improvements in discrimination when age is added to the \gls{FI}, and differential performance by sex, would indicate that the cleaning pipeline preserved the relative informational content of the predictors.
Using the cleaned Wave 1 predictors, models trained on the proxy outcome demonstrated modest discriminative performance, with \gls{AUC} values exceeding random chance and improving consistently when age was included alongside the \gls{FI}. Performance was marginally higher in females than males, mirroring trends reported in the original study. Although absolute \gls{AUC} values were lower than those reported by \cite{moguilnerImportanceAgePrediction2021}, this outcome is expected given the reduced number of available deficit variables, the absence of true mortality linkage, and the intentionally randomised nature of the proxy outcome.


% ---------------------------------------------------------------------------
% ----------------------- end of thesis sub-document ------------------------
% ---------------------------------------------------------------------------