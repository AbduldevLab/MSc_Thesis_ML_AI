% ---------------------------------------------------------------------------
% CHAPTER 5: EMPIRICAL EVALUATION
% ---------------------------------------------------------------------------
% ----------------------- paths to graphics ------------------------

% change according to folder and file names
\ifpdf
    \graphicspath{{5_chapter5/figures/PNG/}{5_chapter5/figures/PDF/}{5_chapter5/figures/}}
\else
    \graphicspath{{5_chapter5/figures/EPS/}{5_chapter5/figures/}}
\fi
% ---------------------------------------------------------------------------

\chapter{Empirical Evaluation}\label{ch:chapter5}

\section{Modelling and evaluation framework}
This section defines the modelling and evaluation setup used throughout this chapter, including model families and designs, analysis samples, outcome definitions, and evaluation procedures, building directly on the prepared datasets from Chapters~\ref{ch:chapter3} and~\ref{ch:chapter4}.

The evaluation is based on participants aged between $50$--$84$ years with a mean age of $66$. To ensure that the prediction task reflects incident (\emph{new onset}) disease, individuals with the target condition present at the start of a transition were excluded, leaving only \emph{at risk} observations for training and testing. This rule was applied consistently across all designs and for both Track A (Any disease outcome) and Track B (disease specific outcomes).

Final analysis samples differed by design and tracks due to data structure. The baseline design (D1) uses a single transition (Wave 1$\rightarrow$Wave 5), with a follow-up period of approximately 8~years. The pooled multi-wave designs (D2 and D3) aggregate all eligible consecutive transitions (W1$\rightarrow$W2, W2$\rightarrow$W3, W3$\rightarrow$W4, W4$\rightarrow$W5), each spanning approximately 2 years, thereby capturing shorter term dynamics. 

Sample sizes and incident rates are summarised in (Table~\ref{tab:analysis_samples}). Track B (disease specific), sample sizes vary by condition. (Appendix~\ref{app:task_b_incident_rates}) provides the number of observations and incident rates for each disease specific model.

% -------- Outcome definitions -------- 
\subsection{Outcome definitions (Track A and Track B)}
Two complementary prediction tasks were defined.

\paragraph{Track A: Predicting any chronic disease onset.}\label{sc:track_a}
Track A uses a binary outcome indicating the first occurrence of any chronic disease during follow-up.
\begin{itemize}
    \item In D1, disease free status at Wave~1 was established using a composite of self-reported conditions (e.g., eye conditions (\texttt{ph105\_x}), cardiovascular conditions (\texttt{ph201\_xx}), diabetes related conditions (\texttt{ph216\_xx}), general chronic conditions (\texttt{ph301\_xx}), limiting long-term illness (\texttt{DISlongterm}), and self-reported chronic illness status (\texttt{ph003})). Incident cases were then defined using any positive \texttt{ICD10} codes from the \gls{WHO} \emph{International Statistical Classification of Diseases and Related Health Problems 10th Revision} at Wave~5 (e.g., mental and behavioural disorders (\texttt{ICD10\_05}), musculoskeletal diseases (\texttt{ICD10\_13})).
		\begin{itemize}
            \item No single \texttt{ICD10\_ANY} variable exists in Wave 1 to establish a clinical baseline. These clinical codes were recorded unevenly across the middle waves, with only three indicators available in Wave 2, two in Wave 3, and one in Wave 4, before a comprehensive set of 16 variables appeared at Wave 5 partially a consequence of the manual low heterogeneity filtering flow described in Chapter~\ref{sc:manual_var_filtering}.
			\item PHYSICAL \& COGNITIVE HEALTH (PH) variables are self-reported responses that reflect prior doctor diagnoses (``has a doctor ever told you''), whereas \texttt{ph003} captures a general self-assessment of long term illness without requiring clinical confirmation
			\item All feature scoring schemes and labels are provided in (Appendix~\ref{app:item_index_scoring}).
    	\end{itemize}
    \item In D2 and D3, \texttt{ph003} was used to both define the outcome and identify the at risk cohort (\texttt{ph003}=0) at the start of each transition, as it is available in every wave and enables consistent application across all transitions. \[\text{Coverage}_{\text{ph003}} = \frac{\sum_{i=1}^{N} \mathbbm{1}\big(\text{ph003}_{i} \neq missing(-1))}{N} \times 100 = 99.91\%\]
    \item An analysis of Wave 5 data compared self-reported illness (\texttt{ph003}) against clinical records (\texttt{ICD10\_ANY}). While raw agreement reached 59.5\%, the \emph{Cohen's Kappa for agreement beyond chance} (Kappa: 0.167) indicates poor consistency. This divergence reveals that 45\% of individuals who perceived themselves as healthy possessed an objective clinical diagnosis, while 38\% of those reporting illness had no corresponding hospital record. These results confirm that \texttt{ph003} serves as a distinct proxy for self-perceived chronic illness status. It captures a subjective signal of health burden that exists independently of formal clinical markers. % ToDo: drop claim - revise: justifying its use when predicting the presence of any chronic disease in the pooled transitions.
\end{itemize}

\begin{table}[ht]
\centering
\caption{Cohort sample sizes by design for Track A}\label{tab:analysis_samples}
\resizebox{\textwidth}{!}{
\begin{tabular}{|l|c|c|c|}
\hline
\textbf{Design} & \textbf{Observations ($n$)} & \textbf{Incident cases ($n$)} & \textbf{Incidence rate (\%)} \\ \hline
D1: Baseline (W1$\rightarrow$W5) & 916 & 248 & 27.07 \\ \hline
D2: Multi-wave pooled & 14,296 & 3,212 & 22.5 \\ \hline
D3: Top-20 (reduced) & 14,296 & 3,212 & 22.5 \\ \hline
\end{tabular}
}
\end{table}

\paragraph{Track B: Disease specific prediction.}\label{sc:track_b}
Track B targets individual chronic disease outcomes, e.g., circulatory disease (\texttt{ICD10\_09}), diabetes (\texttt{ph726\_01}), and functional decline (\texttt{DISlongterm}). For each prediction task, the outcome was defined as the first recorded diagnosis of the specific condition, and participants were excluded if the condition was present at the baseline of the corresponding transition. Scoring details for each disease specific outcome are provided in (Appendix~\ref{app:item_index_scoring}).

% -------- Modeling designs and hyperparameters --------
\subsection{Modelling designs}\label{sc:modelling_designs}
Four supervised learning algorithms were evaluated across all main Track A designs (D1, D2, D3) to compare linear and non-linear model families under the same evaluation framework.

\begin{enumerate}
    \item \textbf{\gls{LR}} --- A linear model providing interpretability through coefficients and odds ratios. In D1 and D2, a fixed configuration was used: \texttt{solver=``liblinear''}, \texttt{C=0.001}, \texttt{class\_weight=``balanced''},\\ \texttt{max\_iter=2000}. In D3, hyperparameters were tuned via \texttt{RandomizedSearchCV} over \texttt{C} (log-uniform $[10^{-4}, 10]$), \texttt{solver=[``saga'']}, \texttt{l1\_ratio} (uniform $[0,1]$), and \texttt{max\_iter} $\in \{1000, 2000, 5000\}$.
    \item \textbf{\gls{XGB}} --- Aka XGBoost, is a gradient boosted tree ensemble capturing non-linearities and feature interactions. In D1 and D2, fixed parameters were used: \texttt{n\_estimators=1500}, \texttt{max\_depth=3}, \texttt{learning\_rate=0.03}, \texttt{min\_child\_weight=5}, \texttt{gamma=0.1}, \texttt{subsample=0.8}, \texttt{colsample\_bytree=0.7}, with class imbalance handled via \texttt{scale\_pos\_weight} (negative to positive ratio). Early stopping (\texttt{early\_stopping\_rounds=50}) on an internal 20\% validation split determined the optimal number of boosting rounds. The model was then refit on the full training set at that iteration. In D3, all tree and regularisation parameters were additionally tuned via \texttt{RandomizedSearchCV}.
    \item \textbf{\gls{RF}} --- A bagged ensemble of decision trees providing robustness through random feature subsampling (\texttt{max\_features=``sqrt''}). Fixed parameters in D1/D2: \texttt{n\_estimators=500}, \texttt{max\_depth=5}, \texttt{min\_samples\_leaf=10}, with class imbalance handled via \texttt{class\_weight=\{0:1, 1:\text{neg/pos ratio}\}}. In D3, \texttt{n\_estimators} (100--800), \texttt{max\_depth} (3--10), \texttt{min\_samples\_leaf} (5--30), and \texttt{max\_features} $\in \{$\texttt{``sqrt''}, \texttt{``log2''}$\}$ were tuned via random search.
    \item \textbf{\gls{MLP}} --- A feedforward neural network with two hidden layers capturing non-linear hierarchical representations. Fixed parameters in D1/D2: \texttt{hidden\_layer\_sizes=(32,16)}, \texttt{activation=``relu''}, \texttt{solver=``adam''}, \texttt{alpha=0.01}, \texttt{learning\_rate\_init=0.001}, \texttt{max\_iter=500}, with early stopping (\texttt{n\_iter\_no\_change=20}, \texttt{validation\_fraction=0.1}). Class imbalance was handled via \texttt{compute\_sample\_weight(class\_weight=\\``balanced'')} passed at fit time, as \gls{MLP} does not natively support \texttt{class\_weight}. In D3, \texttt{hidden\_layer\_sizes} $\in \{(32,), (64,), (32{,}16), (64{,}32), (128{,}64)\}$, \\\texttt{activation} $\in \{$\texttt{``relu''}, \texttt{``tanh''}$\}$, \texttt{alpha} (log-uniform $[10^{-4}, 1]$), and \\\texttt{learning\_rate\_init} (log-uniform $[10^{-4}, 10^{-1}]$) were tuned via random search.
\end{enumerate}

All four models shared the same preprocessing backbone: median imputation (\texttt{SimpleImputer}), variance filtering (\texttt{VarianceThreshold}, threshold = 0.01), and a missingness filter that dropped any feature exceeding 50\% missing values in the training set. Standard scaling (\texttt{StandardScaler}, zero mean and unit variance) was applied only for \gls{LR} and \gls{MLP}, which are sensitive to feature magnitude. Tree-based models (\gls{XGB} and \gls{RF}) were trained on the raw imputed features without scaling. All preprocessing steps were fit exclusively on the training portion (prior to internal train/validation splitting) before being applied to validation and test sets, preventing any information leakage.
 
Track B (disease specific) models used \gls{XGB} exclusively, as it achieved the strongest and most consistent performance across D2 and D3 in Track~A (see Section~\ref{sc:results}). The same model configuration and hyperparameter search space used in D3 was applied in Track~B.

The three primary modelling designs were implemented to address and assess the effect of baseline prediction, longitudinal pooling, and feature reduction with optimisation, respectively.
\begin{itemize}\label{three_designs}
\item[-] \textbf{D1 (Baseline):} A single W1$\rightarrow$W5 transition. Only participants disease free at Wave~1 (using self-reported and derived disease variables, plus explicit \texttt{ICD10} source variables) were included, with incident disease defined by any positive \texttt{ICD10} code at Wave~5 ($n=916$, incidence 27.1\%). Feature selection used \texttt{SelectKBest} ($k=20$) and \gls{CV} used \texttt{StratifiedKFold} (one observation per participant).
\item[-] \textbf{D2 (Multi-wave pooled):} All four eligible consecutive transitions (W1$\rightarrow$W2, W2$\rightarrow$W3, W3$\rightarrow$W4, W4$\rightarrow$W5) were stacked into a single pooled dataset. Disease free status and incident outcome were defined using \texttt{ph003} (self-reported chronic illness) at each transition. A numeric wave transition indicator (\texttt{wave\_transition\_num}) was added as a feature to capture temporal context. The pooled cohort comprised $n=14{,}296$ person wave observations (incidence 22.5\%). Feature selection used \texttt{SelectKBest} ($k=100$), and \gls{CV} used \texttt{StratifiedGroupKFold} to ensure no participant appeared in more than one fold.
\item[-] \textbf{D3 (Top--20 + \gls{HPO}):} Built directly on the D2 cohort and wave structure. The feature set was restricted to the 20 highest ranked predictors by \gls{XGB} gain importance from D2, plus \texttt{wave\_transition\_num}. No additional feature selection step was applied. \gls{HPO} was performed for all four model families using \texttt{RandomizedSearchCV} (200 iterations) within a \texttt{StratifiedGroupKFold} framework.
\end{itemize}
All three designs shared the same participant-level train/test split (80\%/20\%, stratified by outcome), the same missingness filter and variance threshold applied on training data only, and the same five fold \gls{CV} evaluation protocol. Track~B models followed either the D2 or D3 structure depending on the outcome being predicted. Full details are provided in Subsection~\ref{ssc:trackb}.

% ----------------------
% FLOWCHART:
% ----------------------
\begin{figure}[ht]
\centering
\makebox[\textwidth][c]{
\begin{tikzpicture}[node distance=0.9cm and 1.8cm, every node/.style={font=\scriptsize}, >=stealth, thick]
    % Shared pipeline (vertical chain)
    \node[draw, rounded corners, fill=white, minimum width=4cm, align=center] (raw) {\textbf{All Data}};
    \node[draw, rounded corners, fill=gray!10, below=of raw, align=center] (drop) {Drop $>50\%$ missing};
    \node[draw, rounded corners, fill=gray!10, below=of drop, align=center] (imp) {Median Imputation};
    \node[draw, rounded corners, fill=gray!10, below=of imp, align=center] (var) {Variance Threshold (0.01)};
    \node[draw, rounded corners, fill=gray!10, below=of var, align=center] (split) {Participant-level (ID) Split\\80\% train / 20\% test, stratified};
 
    \draw[->] (raw) -- (drop);
    \draw[->] (drop) -- node[right, font=\scriptsize]{train only}(imp);
    \draw[->] (imp) -- node[right, font=\scriptsize]{train fit}(var);
    \draw[->] (var) -- node[right, font=\scriptsize]{train fit}(split);
 
    % Track A branch (left)
    \node[draw, rounded corners, fill=white, left=2.2cm of split, minimum width=2.8cm, align=center] (tracka)
        {\textbf{Track A}\\Any chronic disease onset};
    \draw[->] (split) -- (tracka);
 
    % D1 node
    \node[draw, rounded corners, fill=blue!10, below right=0.2cm and -0.8cm of tracka,
          minimum width=2.8cm, align=center] (d1)
        {\textbf{D1: Baseline}\\W1$\rightarrow$W5; ICD10 outcome\\$n=916$; SelectKBest $k=20$\\StratifiedKFold};
 
    % D2 node (below D1, connected from tracka AND from D1)
    \node[draw, rounded corners, fill=green!20, below=0.9cm of d1, minimum width=2.8cm, align=center] (d2)
        {\textbf{D2: Multi-wave}\\W1$\rightarrow$W2, \ldots, W4$\rightarrow$W5\\ph003 outcome; $n=14{,}296$\\+ Wave indicator\\SelectKBest $k=100$\\StratifiedGroupKFold};
 
    % D3 node (below D2, arrow from D2 showing it is built on D2)
    \node[draw, rounded corners, fill=green!30, below=0.9cm of d2, minimum width=2.8cm, align=center] (d3)
        {\textbf{D3: Top-20 + HPO}\\Same cohort \& waves as D2\\Top-20 XGB gain features\\No additional SelectKBest\\RandomizedSearchCV (200 iter)\\StratifiedGroupKFold};
 
    \draw[->] (tracka) |- (d1);
    \draw[->] (tracka) |- (d2);
    % D3 inherits from D2 (built on top)
    \draw[->] (d2) -- node[right, font=\scriptsize]{built on} (d3);
 
    % Models node (shared by all three designs)
    \node[draw, rounded corners, fill=gray!20, minimum width=3cm, align=center, font=\scriptsize,
          right=5.2cm of d3] (models) {LR, XGB, RF, MLP};
 
    \path (d3.east) ++ (3,0) coordinate (join);
    \draw[->, dashed] (d1.east) -| (join);
    \draw[->, dashed] (d2.east) -| (join);
    \draw[->, dashed] (d3.east) -- (models.west);
 
    % Track B branch (right)
    \node[draw, rounded corners, fill=white, right=2.2cm of split, minimum width=2.8cm, align=center] (trackb)
        {\textbf{Track B}\\Disease specific};
    \draw[->] (split) -- (trackb);
 
    \node[draw, rounded corners, fill=green!10, below=0.6cm of trackb, minimum width=2.8cm, align=center] (b_design)
        {Follows D2 / D3 structure\\(disease specific $y$, predictor add-ons)};
    \draw[->] (trackb) -- (b_design);
 
    \draw[->, dotted, thick] (d2) -- ++(3.5,0) node[above, font=\scriptsize, rotate=90, xshift=1.4cm ]{all $X$} |- (b_design);
    \draw[->, dotted, thick] (d3) -- ++(3.5,0) node[below, font=\scriptsize, rotate=90, xshift=1.7cm ]{top-20 $X$} |- (b_design);
 
    \node[draw, rounded corners, fill=red!10, below=0.7cm of b_design, minimum width=3.2cm, align=center] (b_outcome)
        {Disease specific $y$\\(e.g., Diabetes, Hypertension)\\Multi-horizon};
    % \draw[->] (b_design) -- (b_outcome);

    \node[draw, rounded corners, fill=orange!12, below=0.7cm of b_outcome, minimum width=3.4cm, align=center] (b_addon)
        {Predictor add-on study\\BASE vs ADDON\\$\Delta$AUC and 95\% CI};
    % \draw[->] (b_design) -- (b_addon);

    \node[draw, rounded corners, fill=gray!20, below=0.7cm of b_addon, align=center, font=\scriptsize] (b_mod)
        {XGB only};
    % \draw[->, dashed] (b_addon) -- (b_mod);

    \path (b_design.east) -- ++(0.5,0) coordinate (rightTop);
    \path (rightTop |- b_addon.east) coordinate (rightBottom);
    \draw[-] (b_design.east) -- (rightTop);
    \draw[-] (rightTop) -- (rightBottom);
    \draw[-] (rightTop |- b_outcome.east) -- (b_outcome.east);
    \draw[-] (rightBottom) -- (b_addon.east);

    \path (b_outcome.west) -- ++(-1.2,0) coordinate (leftTop);
    \path (leftTop |- b_addon.west) coordinate (leftMid);
    \path (leftTop |- b_mod.west) coordinate (leftBase);
    \draw[dashed] (b_outcome.west) -- (leftTop);
    \draw[dashed] (b_addon.west) -- (leftMid);
    \draw[dashed] (leftTop) -- (leftBase);
    \draw[->, dashed] (leftBase) -- (b_mod.west);
 
\end{tikzpicture}
}
\caption{Pipeline overview: shared preprocessing applied to all data before branching into Track~A (any chronic disease, D1/D2/D3) and Track~B (disease specific, following D2/D3 structure). D3 is built directly on D2's cohort and wave structure, adding feature reduction and \gls{HPO}.}\label{fig:pipeline_flowchart}
\end{figure}
\clearpage

\textbf{Figure~\ref{fig:pipeline_flowchart} notes}:
\begin{enumerate}[label= (\roman*), leftmargin=*]
    \item All grey pipeline steps (missingness filter, imputation, variance threshold, split) are shared across Track~A and Track~B. All fitting occurs on training data only.
    \item D1 uses a single W1$\rightarrow$W5 transition with \texttt{StratifiedKFold}, D2 and D3 use all four pooled transitions with \texttt{StratifiedGroupKFold} to prevent participant-level leakage across folds.
    \item D3 is built on D2: it uses the same cohort, waves, and wave indicator, but replaces \texttt{SelectKBest} with the top-20 features ranked by \gls{XGB} gain from D2, and adds \gls{HPO} for all model families.
    \item Feature counts after the missingness filter: D1 retains $\approx$570 features, further reduced to $k=20$ by \texttt{SelectKBest} before model training. D2 retains $\approx$261 features, reduced to $k=100$. D3 uses exactly 21 features (top-20 + wave indicator) with no further selection.
    \item Track~B uses \gls{XGB} only, following the D2 or D3 design with a disease specific outcome and explicit exclusion of direct outcome proxies from the predictor set.
\end{enumerate}

% -------- Data splitting and leakage prevention --------
\subsection{Participant-level data splitting and leakage prevention}
To prevent identity based leakage where models memorise participant specific health signatures rather than learning general risk patterns, all splitting was performed at the \textbf{participant level}. Individuals were assigned exclusively to either the training, validation, or test set, ensuring no participant contributed data to more than one split. The overall dataset was first split into 80\% for model development and 20\% as a held-out test set. The 80\% development partition was then further divided 80\%/20\% internally, yielding a final three way allocation of 64\% training, 16\% validation, and 20\% test. Splitting was performed using participant identifiers to ensure group wise separation, with stratification by outcome to preserve class balance across splits. Rumala~\cite{rumalaHowYouSplit2023c} demonstrates in longitudinal brain \gls{MRI} analysis, this subject wise approach prevents overconfident performance and identity confounding, where models learn individual characteristics alongside diagnostic features, ensuring generalisation to truly unseen individuals.

For D1, where each participant contributes exactly one observation, standard \texttt{StratifiedKFold} ($k=5$) was used. For D2 and D3, where participants may appear across multiple wave transitions, \texttt{StratifiedGroupKFold} ($k=5$) was used to ensure that all observations from a given participant were assigned to the same fold during \gls{CV}, preventing within participant data from appearing in both training and validation folds simultaneously.

Three additional leakage controls were applied across all designs:
\begin{itemize}
    \item \textbf{Pipeline isolation:} Imputation medians, variance masks, and feature selection scores were computed on the training partition only. The resulting transformations were then applied unchanged to validation and test data.
    \item \textbf{Temporal ordering:} Predictors were drawn exclusively from the baseline wave of each transition (Wave~$n$) to predict outcomes at the subsequent wave (Wave~$n+1$), eliminating any look ahead bias.
    \item \textbf{Exclusion of direct proxies:} Variables that directly encode the outcome being predicted (e.g., self-reported disease source indicators that feed into the \texttt{ICD10} codes used as outcome, such as \texttt{ph201\_07} and \texttt{ph301\_06} for nervous system diseases (ICD10\_06)), were removed from the predictor set prior to modelling.
\end{itemize}

% -------- Evaluation protocol and metrics --------
\subsection{Evaluation protocol and decision metrics}
Model performance was assessed using a combination of discrimination, calibration, and classification metrics. Models were trained by minimising their respective objective (loss) functions, while performance was evaluated using out-of-sample metrics. The primary metric \gls{AUC}, chosen for its robustness to class imbalance and its interpretability in risk prediction contexts. Model performance was estimated using 5-fold \gls{CV} on the training set. For D2 and D3, this was implemented using \texttt{StratifiedGroupKFold} to ensure participant-level separation across folds, while D1 used \texttt{StratifiedKFold}. Models that required \gls{HPO} (D3), a random search (\texttt{RandomizedSearchCV}) was conducted within the same \gls{CV} framework.

Calibration was evaluated using the Brier score, which measures the accuracy of predicted probabilities, while classification performance was summarised using the F1 score, capturing the balance between precision and recall in the presence of class imbalance (e.g., 22--27\% incidence). Additional metrics, including sensitivity, specificity, and confusion matrices, were used to support interpretation.

For Track B predictor add-on analysis, 95\% \gls{CI}s for performance deltas were estimated via bootstrap resampling to ensure statistical significance.

% -------- Results --------
\section{Main results}\label{sc:results}
This section discusses the main results for Track A~\ref{sc:track_a} and Track B~\ref{sc:track_b}. Performance is first evaluated across the three modelling designs (D1, D2, and D3)~\ref{three_designs}, followed by cross design and model level comparisons.

% -------- Track A: Any disease onset --------
\subsection{Track A: Any disease onset}
Results are presented across the three modelling designs, with tables summarising discrimination (\gls{AUC}), calibration (Brier score), and classification performance (F1), alongside training, validation, and test metrics to assess generalisation.

\subsubsection{Design 1 performance:}\label{ssc:d1}
Table~\ref{tab:d1_results} summarises the performance of the four models on the single transition task (Wave 1 $\rightarrow$ Wave 5). Discrimination is weak with test \gls{AUC} values ranging from $0.53$--$0.58$, and the larger train--test gaps for the tree based models indicate stronger overfitting in the baseline design.

\begin{table}[ht]
\centering
\caption{Predictive performance across model families for D1 (baseline design).}\label{tab:d1_results}
\resizebox{\textwidth}{!}{
\begin{tabular}{lrrrrrrr}
\toprule
Model & CV AUC & Train AUC & Val AUC & Test AUC & Brier Score & F1 Optimal \\
\midrule
Logistic Regression & 0.589 & 0.677 & 0.582 & 0.571 & 0.245 & 0.411 \\
XGBoost & 0.569 & 0.825 & 0.563 & 0.544 & 0.240 & 0.398 \\
Random Forest & 0.579 & 0.871 & 0.565 & 0.534 & 0.231 & 0.440 \\
MLP & 0.536 & 0.664 & 0.552 & 0.582 & 0.241 & 0.419 \\
\bottomrule
\end{tabular}
}
\end{table}

\subsubsection{Design 2 performance:}\label{ssc:d2}
Table~\ref{tab:d2_results} shows the results when using all consecutive transitions (pooled multi-wave design). All models improve substantially compared to D1~\ref{ssc:d1}, with test \gls{AUC} values ranging from $0.69$ to $0.72$.

\begin{table}[ht]
\centering
\caption{Predictive performance across model families for D2 (multi-wave design).}\label{tab:d2_results}
\resizebox{\textwidth}{!}{
\begin{tabular}{lrrrrrrr}
\toprule
Model & CV AUC & Train AUC & Val AUC & Test AUC & Brier Score & F1 Optimal \\
\midrule
Logistic Regression & 0.715 & 0.728 & 0.715 & 0.709 & 0.217 & 0.460 \\
XGBoost & 0.716 & 0.736 & 0.719 & 0.715 & 0.214 & 0.468 \\
Random Forest & 0.711 & 0.740 & 0.717 & 0.711 & 0.212 & 0.452 \\
MLP & 0.698 & 0.749 & 0.706 & 0.699 & 0.208 & 0.455 \\
\bottomrule
\end{tabular}
}
\end{table}

\subsubsection{Design 3 performance:}\label{ssc:d3}
Table~\ref{tab:d3_results} presents performance using only the 20 most informative predictors (derived from \gls{XGB} model trained on D2~\ref{ssc:d2}) with \gls{HPO} applied across all models. Discrimination remains effectively unchanged compared to D2, with test \gls{AUC} values tightly clustered around $0.71$.
\begin{table}[ht]
\centering
\caption{Predictive performance across model families for D3 (tuned design).}\label{tab:d3_results}
\resizebox{\textwidth}{!}{
\begin{tabular}{lrrrrrrr}
\toprule
Model & CV AUC & Train AUC & Val AUC & Test AUC & Brier Score & F1 Optimal \\
\midrule
Logistic Regression & 0.716 & 0.718 & 0.717 & 0.712 & 0.212 & 0.459 \\
XGBoost & 0.718 & 0.736 & 0.719 & 0.713 & 0.209 & 0.462 \\
Random Forest & 0.717 & 0.766 & 0.718 & 0.713 & 0.205 & 0.462 \\
MLP & 0.713 & 0.743 & 0.710 & 0.712 & 0.207 & 0.467 \\
\bottomrule
\end{tabular}
}
\end{table}

% -------- Cross design and model comparison --------
\subsection{Cross-design comparison}
The best test \gls{AUC} increased from $0.582$ in D1~\ref{ssc:d1} (achieved by \gls{MLP}) to $0.715$ in D2~\ref{ssc:d2} by \gls{XGB}, representing an improvement of $0.133$, demonstrating the benefit of incorporating pooled longitudinal transitions.

Reducing the feature set to the top 20 predictors in D3 maintained this gain, with \gls{XGB} achieving a test \gls{AUC} of $0.713$, only $0.002$ lower than D2, confirming that most of the predictive signal is concentrated in a small subset of variables, and that performance is robust to feature reduction.

Across designs, \gls{CV} performance also improves and stabilises:
\begin{itemize}
    \item[-] D1: $0.589 \pm 0.072$ (\gls{LR})
    \item[-] D2: $0.716 \pm 0.007$ (\gls{XGB})
    \item[-] D3: $0.718 \pm 0.006$ (\gls{XGB})
\end{itemize}

The reduction in standard deviation from D1 to D2 and D3 reflects improved stability associated with the pooled multi-wave design and more consistent learning signal across transitions, although differences in data structure across designs should be considered when interpreting cross-design variability.

% -------- Model comparison --------
\subsection{Model comparison}
Once temporal structure is introduced (D2 and D3), differences between model families are modest. Test \gls{AUC} values cluster in the range ($0.69$--$0.72$), with no single model class dominating performance. \gls{XGB} achieved the highest test \gls{AUC} in both D2 ($0.715$) and D3 ($0.713$), though the margin over \gls{LR} was narrow ($\leq 0.006$). The fact that the linear \gls{LR} remained consistently competitive ($0.709$ in D2~\ref{ssc:d2} and $0.712$ in D3~\ref{ssc:d3}) suggests that risk signals in the ageing population are primarily linear. \gls{MLP} slightly underperforms in D2~\ref{ssc:d2} ($0.699$) but matched other models in D3~\ref{ssc:d3} ($0.712$), indicating improved performance when the feature space is reduced to the most informative predictors. This suggests that individual health indicators, such as physical pain (\texttt{ph501}) or medication counts (\texttt{MDmeds4}), contribute largely and independently to the risk score, with limited additional benefit from modelling complex non-linear interactions.

Figure~\ref{fig:generalisation_gap} compares generalisation gaps (Train $-$ Test) \gls{AUC} across all three designs. The average train-test \gls{AUC} gap (across all four models) drops from $0.202$ in D1 to $0.030$ in D2 and $0.028$ in D3 --- a reduction of over $85\%$. This demonstrates that the pooled multi-wave design effectively reduces overfitting.
D1 exhibits the largest gaps, consistent with weaker signal and higher variance in estimates. In contrast, D2 and D3 show much smaller gaps, indicating improved stability. Tree based models (\gls{XGB} and \gls{RF}) exhibit the largest gaps in D1, consistent with the small sample size. In D2 and D3, \gls{XGB} achieves the smallest gaps ($0.021$ and $0.023$ respectively), while \gls{RF} shows comparatively larger gaps ($0.029$ and $0.053$). The strong performance of \gls{XGB} in D3 is expected, as the top-20 feature set was selected using \gls{XGB} gain importance from D2. Nevertheless, \gls{XGB} also generalises well in D2, suggesting that its boosting mechanism is effective at learning stable patterns from pooled longitudinal data. In contrast, \gls{RF} may be more prone to overfitting even with the pooled design, possibly due to its reliance on bagging and random feature selection, which may capture informative predictors less efficiently than boosting.

\begin{figure}[ht]
    \centering
    \includegraphics[width=0.9\textwidth]{gen_gap_fig.png}
    \caption{Train–test generalisation gaps across models and designs. $0.05$ marks a conventional heuristic threshold for acceptable error in AUC. Gaps below this level indicate that the model generalises well to unseen data.}\label{fig:generalisation_gap}
\end{figure}
The figure also highlights that model level differences in generalisation gap are more pronounced in D1~\ref{ssc:d1}, whereas D2~\ref{ssc:d2} and D3~\ref{ssc:d3} exhibit convergence across model families, suggesting that data structure plays a larger role than algorithmic choice in controlling overfitting. 

% -------- Track B: Disease specific --------
\clearpage
\subsection{Track B: Disease specific}\label{ssc:trackb}
Track~B focuses on individual chronic disease outcomes. Each model targets a single condition using the pooled multi wave cohort structure, participant level splitting, and \gls{XGB} classifier established in D2 and D3. Twenty four disease specific outcomes were evaluated using two feature strategies: all eligible predictors retained after the 50\% missingness filter (for example, $261$ features for functional decline and $351$ for circulatory disease), and a restricted top 20 set derived from \gls{XGB} gain importance.

The outcomes span key domains of ageing related health, including cardiovascular conditions, functional decline, mental health, and metabolic disease, ensuring that the models cover a broad range of clinical relevance in the \gls{TILDA} cohort.

\subsubsection{Outcome categories}
\begin{itemize}
    \item \textbf{Clinical outcomes (ICD10 codes):} Circulatory disease, endocrine and metabolic diseases, eye diseases, and musculoskeletal diseases, derived from \gls{WHO} \texttt{ICD10} codes. These codes were only available comprehensively from Wave 5, with limited coverage in earlier waves, as described in Subsection~\ref{ssc:d1}. All source predictor variables that directly encode each condition were excluded to prevent leakage.
    \item \textbf{Functional and symptom outcomes:} Functional decline, falls and balance, chronic pain, hearing loss, and urinary incontinence, from self-reported health variables. These outcomes are available across multiple waves and support pooling across multiple transitions (e.g., W2$\rightarrow$W4, W3$\rightarrow$W4$\rightarrow$W5 and W2$\rightarrow$W4).
    \item \textbf{Family history indicators (\texttt{ph726\_xx}):} Variables recording whether a participant reports that a family member has been diagnosed with a condition, covering diabetes, high cholesterol, hypertension, heart disease, osteoporosis, Alzheimer's disease, cancer, and depression. These are not incident diagnoses of the participant and represent a distinct prediction target: estimating the likelihood of a participant newly reporting a family history item at follow-up. These variables were retained through the manual filtering process described in Section~\ref{sc:manual_var_filtering}.
    \item \textbf{Medication and procedure proxies:} Antidepressant and antihypertensive prescription, pain analgesia, cholesterol medication, doctor confirmed hypertension, and cardiac procedure. These are downstream markers of clinical decision making. When used as prediction targets, they capture risk signals linked to eventual treatment rather than raw disease onset. As noted in (Appendix~\ref{app:task_b_incident_rates}), incidence rates for medication proxies vary widely, from $1.4\%$ for cardiac procedure to $41.6\%$ for doctor confirmed hypertension diagnosis (though this is based on a very small cohort of $n = 250$). 
\end{itemize}

\subsubsection{Overall performance}
Figure~\ref{fig:trackb_scatter_plot} plots train \gls{AUC} against test \gls{AUC} for all disease specific models, coloured by outcome category. The diagonal represents perfect generalisation (train equals test), while dashed lines at $0.5$ on both axes mark chance level performance. Points close to the diagonal indicate stable learned signal, whereas points far above it, with much higher train than test \gls{AUC}, indicate overfitting. Overall, test \gls{AUC} values range from approximately $0.45$ to $0.72$, reflecting substantial variation driven mainly by outcome type and incidence rate rather than model choice.

Functional and symptom outcomes were the most reliable across both feature strategies. Functional decline ($n = 14{,}296$, incidence 22.5\%) achieved a test \gls{AUC} of $0.713$ with a small gap between train and test, consistent with the strong and persistent frailty signal identified in the literature. Fear of falling ($n = 18{,}114$, incidence 14.0\%) achieved $0.721$, the highest test \gls{AUC}, closely followed by antihypertensive prescription, reflecting the well documented link between functional limitation and falls risk in \gls{TILDA}. Hearing loss ($n = 10{,}780$, incidence 15.1\%) reached $0.678$, and urinary incontinence ($n = 16{,}208$, incidence 8.4\%) achieved $0.682$, both with gaps below $0.10$. These outcomes benefit from consistent measurement across waves, sufficient event counts, and clear physiological pathways, which support more stable longitudinal prediction.

Medication proxies achieved competitive test \gls{AUC} values. Antihypertensive prescription ($n = 19{,}845$, incidence 4.5\%) reached $0.718$, and antidepressant prescription ($n = 4{,}378$, incidence 3.2\%) reached $0.702$, despite low incidence. Both models showed train \gls{AUC} above $0.99$, indicating overfitting to training specific patterns rather than fully generalisable risk patterns. The test results shows that the models can discriminate between cases and non cases, although part of this performance likely comes from established treatment profiles. For example, a participant already receiving antihypertensives at baseline has a clinical profile strongly associated with future prescription continuation. These models are therefore informative for risk stratification but should not be interpreted as predicting new disease onset.

Clinical \texttt{ICD10} outcomes showed moderate and variable performance. Circulatory disease ($n = 14{,}539$, incidence 12.2\%) and eye diseases ($n = 4{,}396$, incidence 10.1\%) were the strongest, reaching test \gls{AUC} values of $0.626$ and $0.653$, respectively. This aligns with findings that cardiovascular outcomes in \gls{TILDA} are supported by well established clinical predictors. Musculoskeletal diseases ($n = 4{,}269$, incidence 6.5\%) and endocrine and metabolic diseases ($n = 4{,}117$, incidence 8.8\%) performed near chance, with test \gls{AUC} values of $0.507$ and $0.515$ under the all features strategy, alongside train \gls{AUC} values of $1.000$ and $0.967$. This pattern indicates that the models memorised training labels without learning signal that transfers to new data. This issue is discussed further in Section~\ref{ssc:trackb_overfitting}.

Family history models produced the most variable and least reliable results. Using all features, test \gls{AUC} ranged from $0.513$ for cancer ($n = 3{,}971$, incidence 13.4\%) to $0.665$ for high cholesterol ($n = 4{,}312$, incidence 12.0\%), while training \gls{AUC} reached $1.000$ for several outcomes such as Alzheimer's disease and cancer, indicating severe memorisation. Switching to the top-20 feature set consistently degraded performance, with several models dropping below $0.50$. The root cause is the indirect nature of the target: family history variables capture a participant's reported knowledge of a relative's diagnosis, rather than an outcome experienced by the participant. As a result, there is no direct causal pathway linking participant level predictors to these outcomes, limiting the model's ability to learn generalisable patterns. What is learned from the training data therefore doesn't transfer well to unseen data.
\begin{figure}[ht]
    \centering
    \includegraphics[width=0.9\textwidth]{disease_specific_fig.png}
    \caption{Train \gls{AUC} vs Test \gls{AUC} for all Track~B disease specific models, coloured by outcome category. Circle markers represent all feature models and cross markers represent top-20 feature models, with a line connecting the two feature strategies per disease. The diagonal dashed line marks perfect generalisation (Train $\approx$ Test). Dashed lines at $0.5$ on both axes indicate chance level discrimination. Confidence ellipses illustrate within category spread.}\label{fig:trackb_scatter_plot}
\end{figure}

\subsubsection{Incidence and predictability}
Cohort sizes and incidence rates for each outcome are provided in Appendix~\ref{app:task_b_incident_rates}. Incidence rate appears to be the most consistent driver of model stability in Track~B. Outcomes with incidence above approximately $12\%$, particularly when measured consistently across waves, tended to achieve test \gls{AUC} values above $0.65$. In contrast, outcomes with incidence below approximately $7\%$ generally produced near chance test \gls{AUC} and large gaps between train and test regardless of feature strategy.

A model trained on few positive examples cannot learn stable decision boundaries. Class imbalance weighting via \texttt{scale\_pos\_weight} partially compensates, but when positive cases are very sparse, the model can achieve high training scores by memorising those few cases without generalising. This is why Train $= 1.000$ appears for musculoskeletal diseases, Alzheimer's disease, and cancer in the all features models, none of which exceed test \gls{AUC} of $0.54$ despite high training performance.

Doctor confirmed hypertension is an outlier worth noting. Its incidence rate of $41.6\%$ is the highest in Track~B, and it achieved test \gls{AUC} of $0.718$. However, the cohort is very small at $n = 250$, making this estimate less reliable. This result should be interpreted with caution, as a cohort of this size can produce highly variable test scores depending on the specific participants allocated to the test split.

\subsubsection{Overfitting and feature reduction}\label{ssc:trackb_overfitting}
For outcomes where the all features strategy produced train \gls{AUC} substantially above $0.90$ alongside near chance test \gls{AUC}, the top-20 strategy provides a more reliable estimate of generalisable performance, even when absolute performance remains modest. Musculoskeletal diseases moved from train $1.000$ / test $0.507$ to train $0.761$ / test $0.548$ under feature reduction. Endocrine and metabolic diseases similarly improved from $0.515$ to $0.561$. These are not large gains, but they indicate a more calibrated model that has learnt from signal rather than noise. In contrast, for outcomes where both strategies produced similar test \gls{AUC} with small gaps, such as functional decline, fear of falling, and hearing loss, the feature reduction confirms stable learning rather than addressing overfitting.

For family history targets, feature reduction worsened rather than helped. With only 20 features, the model is further constrained in its ability to identify even proxy signal for family reported diagnoses. This is consistent with the broader point that the target type, rather than the feature count, drives instability in these models.

The overall pattern across Track~B is consistent with the preprocessing decisions made in Chapter~\ref{sc:manual_var_filtering}. Variables were excluded during the manual filtering stage if they exceeded the missingness threshold, exhibited constant values, or had a single response category dominating more than approximately 90\% of observations. This process improved overall signal quality, but it also reduced the number of available predictors for outcomes that are inherently sparse or indirectly measured. Outcomes with rich, well distributed predictor signals, such as functional decline and fear of falling, benefited most. Outcomes dependent on rare or indirect signals, such as Alzheimer's family history or cardiac procedure, remained constrained by both the target definition and the reduced predictor space. The results in Figure~\ref{fig:trackb_scatter_plot} should therefore be interpreted not only as a function of modelling choices, but as a reflection of data availability and the structural properties of each outcome in \gls{TILDA}.

% -------- feature importance --------
% This groups ALL interpretability + evaluation together, ROC / PR discussion? SHAP / gain? consistent features (i iwll share all the top featires from all models in d1 and the top 20 features in d2 and of course d3 sed xgb from dz and so did track b.. so i will share all of them), feature ablation (domain contribution - be able to link this back to our chpater 3 of thematic domains), and then a discussion of the most important features and their clinical relevance, as well as the consistency of features across models and designs. I will also discuss the top features for the disease specific models in track b, and how they relate to known risk factors for those diseases. I will also discuss any surprising features that emerged as important, and what they might indicate about underlying mechanisms or potential intervention targets.
% I think here is where we elaborate on our add on implementation where we target spefic predictors and see how they perform wihtout and with , here we can talk about teh 95% CI. method was where te outocme used and add on teh waves that existed for both where used only..we can add a table to represnt teh etric we recorded for this ad don but im concius of not making teh section mian reults lengthy.. because that would mena for us to add any vairbale mentoend to teh scoring appendix... too , we can add the tbale of teh ci scores in appenidx in atable but of course we would still have to talk about the mian reults here from it and also to make things kore inetretsing teh top 20 fetaires deirbed from d2 which is used in d3 and track b .. i also computed 95ci in the add on model..at least it gives us a talking point and more importabtly a mentioning point about the top 20featires that were used which we dindt really speak about in our previous sections of this chapter.. i also do This section evaluates BASE and BASE+ADDON test performance within key subgroups: sex,age and education strata so maybe the predictors we mention here whcih you will decide base din the results, we can furtehr deep dive furtehr fractionally into these. Im just brainstorming but of course im more htne happy for any better recoomendartions for this section.
% Also we need to relate the top 20 observed mainly from teh xgb model and relate it bakc to the featires / types chosen for track b specific disease prediction... example teh top 20 model shows medication fetaires ocntoribute the best thus we could connect this to teh medicaation type we did inside teh track B subseciton inside main reulsts section? so again lets observe all the ouputs we have and communciate our weffective analysis and understandings (findings) in a clear concise way that is not too overwhelming for the reader but also gives them a good insight into the data and the models. I think we can also use this section to talk about the limitations of our interpretability methods and how they might affect our conclusions about feature importance. For example, we can discuss how SHAP values can be influenced by feature correlation and how this might lead to misleading interpretations in some cases. We can also talk about how the top features identified by our models might not necessarily be causal risk factors, but rather proxies for underlying processes that we haven't measured directly. This would be an important caveat to include when discussing the clinical relevance of our findings.
% we should also circle back in the obsrved feature imporitance back to our lietrtaire review chapter very important some papers we tlaked baout how certain features/aspects contribute exmaple getting up from a chair... in our top 20 kodel we can see a variable 'Difficulty getting up from a chair after long sitting' which was in the top 20 so again as its important to talk about the addons right and tehre observed 95 Ci & top20 features its also importnat to circle abck and relate thinsg connect teh dots ir else or tehsis will ne fragmneted and disjointed. We can also use this section to discuss potential intervention targets based on the most important features identified by our models. For example, if certain physical function indicators are consistently among the top predictors for functional decline, this could suggest that interventions aimed at improving physical function might be effective in reducing the risk of decline. Similarly, if certain medication use patterns are strong predictors for specific disease outcomes, this could inform clinical decision making around medication management in older adults. Overall, this section should provide a comprehensive analysis of feature importance across our models and designs, linking these findings back to the broader literature and discussing their implications for understanding ageing related health risks and potential interventions. (cite papers where necerssary even if we have already cited them in the literature review chapter, we can refer back to those citations when discussing specific features and their relevance to known risk factors or mechanisms in ageing related health outcomes).

\section{Model interpretability and feature importance}\label{sc:feature_importance}
% Note i do have two images for top 20 features for D1 and D2 - which contain features / variables in y-axis and scores in x-axis...also for the add on i have a plot for it too (y-axis varibales and x-axis the scores), so we can save time and not include latex code i can just insert these images to appenidx right? they are sorted in ascending order with highest scores being last at te bottom of teh figure..
% Please trim anything that repeats or is not directly relevant to the main findings, and focus on providing a clear and concise narrative that highlights the key insights from our feature importance analysis while acknowledging the limitations of our methods and data. The goal is to provide a nuanced interpretation of our results that informs both scientific understanding and potential clinical applications, without overstating the implications or drawing causal conclusions from observational data. avoid hyphens please and gls{ICD10} doesnt exist so stop doing it..
% To note the style of writing should be in 'the style of writing should be formal and impersonal, but attempting to interest a technically aware reader. Very detailed discussion of datasets, simulations, software, or intricate derivation of analytical results may be more appropriate in appendices.' avoid we etc...
This section examines which predictors drive chronic disease risk. Rather than listing every feature, we focus on three empirical findings: (1) predictive signal is concentrated in a very small subset of variables, (2) the most important predictors shift from clinical biomarkers to indicators of accumulated health burden when moving from a long-term to a short-term prediction framework, and (3) no single additional variable adds independent predictive value once that core set is included. Detailed feature importance barplot and full add-on results are in (Appendix~\ref{app:top20_features} and Figure~\ref{fig:add_on_fig}).% change this last sentence we are delaing wiht one figure for add on here and the two figs for top 20 d1/d2 % Lets add a combination of two tables for feature importance where the first table shows the common top features present in d1, the common top features present in d2(this will go in appendix. the table about teh add-on i think we can also include 10 only should suffice) - this line chnage 'Rather than listing every feature'. Also the 3 points we mention needs to be more robust and corrected one mention and connect about the top featires and how the thematic domian related wiht them.. of coure anothe rmention shold be about add ons which is the predictors... the 3rd point i will let you decide... regarding what tables to dispplay in the appendices.. we can also compute conciley and neatly the top 20 featire and their socres across the model families for d1 and d2 because essentially d3 and tarck b uses d2 xgb top20 right.. or of you feel its better to just share the top commpon features across model fmialies in d1 collectively and also for d2 we can aswell but imm not sure how we will compute the importance scores if we will merge teh common top featires across the models which i think we should do... maybe when we speak about speic top featires we compute the importance in text rather? leave placeholder for teh tables appendix i will add it myself..
\subsection{Design 1: Clinical biomarkers dominate}\label{ssc:fi_d1} % re-verify of this is true and % remove lable if not used..
In D1, the top-20 features selected by \texttt{SelectKBest} were dominated by cardiovascular and anthropometric measurements from the Wave~1 health centre assessment. Blood pressure variables, covering seated and standing systolic and diastolic readings and their means, appeared prominently across all four models. Anthropometric frailty markers, hip and waist circumference and body mass index, were also consistently ranked, alongside trail making test performance time as a marker of cognitive processing speed. Social engagement, functional disability, walking difficulty, and socioeconomic group contributed additional signal. % great paragraph.

This pattern directly reflects what Chapter~\ref{ch:chapter2} identified as the strongest predictors of chronic disease and mortality in older adult cohorts. Blood pressure is a well established cardiovascular risk marker~\cite{matthewsPredictingMortalityIrish2022}, while anthropometric measures relate to the frailty obesity pathway identified in \gls{TILDA}~\cite{huangAgeModifyAssociations2019}. The trail making test time, capturing executive function and processing speed, connects to the cognitive reserve framework discussed in Chapter~\ref{ch:chapter2}~\cite{boyleBrainpredictedAgeDifference2021}. The dominance of objectively measured variables in D1 reflects the availability of a full Wave~1 health centre assessment, providing clinical biomarkers not consistently available in later waves. % great paragraph again , i would tighten some of the langauge used so that the reader understands more easily i think. small lanagueg tweaks maybe needed tahts all.

Across model families, the same core features appeared with minor ordering differences. Blood pressure variables ranked first for \gls{XGB} and \gls{RF}, while \gls{LR} produced a broader spread across the top 20 including social activity and socioeconomic group. The \gls{MLP} permutation importance confirmed hip circumference and seated systolic blood pressure mean as the two strongest features, consistent with the tree-based rankings~\ref{fig:top20_d1}. % yes here i tocuhed base on this so we wont have a figure... depdning on what we decode wth teh comment i left abpve.. i would say we just compute the importance scores for this instead.... in text.. but we need to be careful becaus ethis senetnce says what was teh common across teh desigsn rigth but still we can still compute teh scpre for each design when we mentin featuire x...

\subsection{Design 2: Medication burden and self-rated health emerge}\label{ssc:fi_d2}% remove lable if not used..

Shifting from D1 to D2 produced a clear change in the importance landscape. Blood pressure variables, which dominated D1, no longer appeared in the top 20 for any model in D2. Instead, medication burden and self-rated health became the primary drivers. % great paragraph.

Medication count excluding supplements ranked first in \gls{XGB}, \gls{RF}, and \gls{MLP} in D2. Polypharmacy indicators (five or more medications), total medication counts including supplements, and extended medication burden measures collectively occupied much of the top 10 across all four models. This shift reflects the change in outcome from \texttt{ICD10} coded hospitalisation over 8 years (D1) to self-reported chronic illness over 2-year transitions (D2). Over shorter windows, medication burden is a more proximal indicator of health deterioration: a participant already taking multiple medications at baseline is much more likely to report chronic illness at the next wave, and the signal is present and consistent across all four transitions. % great intuition i loev this some lanaguge used cna be simplified i belive some word choices i mean.

Self-rated general health was the most important single feature for \gls{LR} and \gls{RF} in D2, and appeared in the top six for all models. This is one of the most replicated predictors of subsequent morbidity and mortality in older adult cohorts~\cite{moguilnerImportanceAgePrediction2021}, and its prominence here validates our predictive framework choice. Comparative self-rated health, a question asking participants how their health compares with others of the same age, also appeared in the top 20 across all models. % I love this paragprah great interpretation and eay to understand.

Pain related variables contributed substantially in D2. Chronic pain limiting activities, pain severity, and any chronic pain indicator all ranked consistently. Chapter~\ref{ch:chapter2} identified chronic pain as a marker of underlying functional decline and a strong correlate of subsequent disease burden~\cite{lairdPhysicalActivityDepression2023}. Functional disability items, specifically difficulty getting up from a chair and stopping, and aggregate physical limitation counts, rounded out the top predictors. Healthcare utilisation variables, general practitioner visit frequency and outpatient visit frequency, also appeared, reflecting that participants who engage heavily with the health system are already at elevated risk of incident chronic illness~\ref{fig:top20_d2}. % replace the word stopping with steadiness feeling hwne getting up frm a chair instead?

\subsection{Cross-model consistency and the D3 feature set}\label{ssc:fi_consistency} % remove lable if not used.. also shorten the title here ALSO ITS IMPORTAMNT IN THE APNEIDX WHERE WE SHOW THE TOP 20 TABLES DONT INCLUDE TE VARIBALE CODE NO NEED THAT WAS DONE ONLY FOR THE MIAN REULSTS SECTION WHERE NEEDED FOR THIS THERE SNO NEED WE CAN JUST COMPUTE TEH LABLE AND THE IMPORTANCE SCORE FOR EACH ..... 

A notable finding is the high degree of agreement across model families in D2 regarding which features matter. The top five features by \gls{XGB} gain, \gls{RF} Gini importance, \gls{LR} absolute coefficient magnitude, and \gls{MLP} permutation importance all include medication burden and self-rated health, with only minor ordering differences. This cross-model agreement strengthens confidence that these represent genuine risk signals rather than model-specific artefacts. % i agree strong paragraph. I think its best to just represnt teh top team plain in appenidx across the modle famiies for d1 and d2 only becaus ei see we hard print top 20 used in d3/track b in this section itself which is fine teh rest can go into appendix.. only showing the comoon features across designs will defeat alot of purpose of what we spoke about teh reader wont be able to verufy this.

The D3 and Track~B top-20 feature set was derived directly from \gls{XGB} gain importance in D2 and applied unchanged across all four D3 model families and all Track~B disease-specific models. Table~\ref{tab:top20_features_gain} lists this feature set with descriptions and thematic domain assignments (following the seven domains described in Section~\ref{sc:manual_var_filtering}). % I love this thank you a lot.

\begin{table}[ht]
\centering
\caption{Top-20 features from \gls{XGB} gain in D2, used in D3 and Track~B.}\label{tab:top20_features_gain}
\small
\resizebox{\textwidth}{!}{
\begin{tabular}{lll}
\toprule
Feature label & Description & Thematic domain \\
\midrule
Medication count (excl. supplements) & Total regular medications & Physical/Clinical \\
Chronic pain (often troubled) & Frequency of pain experienced & Functional/Symptom \\
Polypharmacy (excl. supplements) & Taking five or more medications & Physical/Clinical \\
Medication count (incl. supplements) & Number of regular medications & Physical/Clinical \\
Medication count & All reported medications & Physical/Clinical \\
Self-rated general health & Excellent, very good, good, fair, or poor health & Physical/Clinical \\
Physical impairment count & Number of activity limitations from mobility items & Functional \\
Activity limitation & Health limits kind or amount of activities & Functional \\
Pain limits activities & Chronic pain restricts daily activities & Functional/Symptom \\
GP visit frequency & Number of GP visits in last 12 months & Physical/Clinical \\
Large muscle \gls{ADL}/\gls{IADL} index & Composite of large muscle daily activities & Functional \\
Pain severity & Level of pain reported & Functional/Symptom \\
Flu vaccination & Had a flu vaccination in past 12 months & Lifestyle \\
Walking steadiness & Steadiness when walking & Functional \\
Chronic pain indicator & Any chronic pain present & Functional/Symptom \\
Chair rise steadiness & Steadiness when getting up from a chair & Functional \\
Outpatient visit frequency & Number of outpatient visits in last 12 months & Physical/Clinical \\
Chair rising difficulty & Difficulty getting up from a chair after long sitting & Functional \\
Stooping difficulty & Difficulty stooping, kneeling, or crouching & Functional \\
Comparative self-rated health & Health compared with others of same age & Physical/Clinical \\
\bottomrule
\end{tabular}
}
\end{table}

The feature set spans primarily two domains: physical and clinical health (medication counts, self-rated health, healthcare utilisation), and functional status (mobility difficulty, pain, physical limitation). Nutritional and metabolic biomarkers, psychological and cognitive measures, and sociodemographic variables are absent. This is not a limitation of the pipeline but a result of the signal concentration: once medication burden and self-rated health are included, additional domain predictors contribute negligible independent information within two-year transitions in this cohort. This finding directly addresses one of the gaps identified in Chapter~\ref{ch:chapter2}, namely the tendency for models to over-rely on clinical measurements~\cite{davisComparisonGaitSpeed2021}. Here, the combination of self-reported functional items and medication patterns matched or exceeded the predictive value of objective biomarkers, supporting the use of survey-based predictors in clinical screening tools. % REVISE THIS PARAGRAPH , IF NOT NECESSARY PLEASE DROP

Connecting this back to Track~B, the medication-dominated top-20 feature set explains why medication and diagnosis proxy outcomes such as antihypertensive prescription and doctor-confirmed hypertension achieved the highest Test \gls{AUC} values in Track~B. The model's predictors are already largely medication based, giving it a natural advantage when the outcome is also a medication or treatment indicator. This is a structural alignment rather than a finding about disease biology, and should be interpreted accordingly. % I AGREE THIS IS A GREAT POINT BUT TOWARDS THE END MAYBE CHANGE HOW WE COMMUNICATE WHAT WE SAID ONLY TEH END OF THIS PARAGRPAH

\subsection{Track B predictor add-on analysis}\label{ssc:fi_addon} % DROP LEBALE IF NOT USED

The add-on analysis evaluated the incremental contribution of individual predictors to functional decline prediction using a BASE versus BASE+ADDON framework. For each candidate, a model was trained without the feature (BASE) and then with it included (BASE+ADDON). The delta in \gls{AUC} and average precision was estimated with bootstrapped 95\% \gls{CI}s ($B = 2{,}000$ resamples). Full results are provided in (Figure~\ref{fig:add_on_fig}). The add-ons evaluated span lifestyle, socioeconomic, cognitive, nutritional, and clinical domains, including several features from the thematic domains identified in Chapter~\ref{ch:chapter3} that were not represented in the top-20 feature set. % i love this paraggrpah, great!

The central finding is that no single add-on produced a statistically significant improvement in discrimination or calibration for functional decline. Every \gls{AUC} delta \gls{CI} crossed zero. Selected results from the most clinically informative candidates are discussed below. % give reason to why , example it makes sense if we have 261 predcitors going inot teh modela fter missnginess etc all applied 260 predcitor for base vs 260+1 will cause small chnages so awkknwldge that bit claim this wasnt teh purpose of the add-on we knew this and expected small chnages in dleta auc etc  but we wnated to really observe teh direction of preditor from teh tehmatic domain plays it role really overall

% organse below into itemsizes \item[-] with i, ii etc more vsiually appelaing.. also re check this and make sure these ar eteh absoluet we should actually mentiona nd are worhty to mention..
\textbf{Education} added the largest observed positive delta: Test \gls{AUC} increased from $0.711$ to $0.719$ with a \gls{CI} of $[-0.002, 0.018]$. The upper bound of $0.018$ suggests a real but statistically uncertain benefit. Education is an established determinant of health literacy, healthcare access, and chronic disease risk in older adults~\cite{mccrorySocialDisadvantageSocial2016}, and this result is consistent with the social disadvantage literature reviewed in Chapter~\ref{ch:chapter2}. The uncertainty may reflect collinearity with socioeconomic and healthcare utilisation predictors already in the BASE set.

\textbf{Difficulty walking 1 km} was the highest performing BASE configuration in the study (Test \gls{AUC} $0.714$, CV \gls{AUC} $0.726 \pm 0.009$), and adding this mobility item as an add-on produced no further improvement (delta \gls{CI}: $[-0.007, 0.007]$). This indicates the feature carries strong signal on its own, but once a rich BASE model of functional limitation and medication burden is present, the specific difficulty with 1~km walking adds nothing new. Chapter~\ref{ch:chapter2} identified gait speed and mobility tests as primary frailty indicators in TILDA~\cite{davisComparisonGaitSpeed2021}; the high BASE performance when this feature is available confirms its importance, while the null delta confirms its signal is shared with the existing feature set.

\textbf{Chair rising difficulty}, included in the top-20 feature set, also showed a null add-on delta ($[-0.012, 0.002]$) when tested as an add-on. Chapter~\ref{ch:chapter2} specifically cited chair stand performance as a key mobility and strength indicator~\cite{davisComparisonGaitSpeed2021}. Its null marginal contribution as an add-on, despite its importance as a standalone feature, illustrates how correlated predictors of the same functional pathway (pain, steadiness, mobility difficulty) share their predictive variance.

\textbf{Grip strength} produced a small positive delta of $+0.004$ (\gls{CI}: $[-0.004, 0.012]$). Chapter~\ref{ch:chapter2} identifies grip strength as one of the primary markers of frailty and decline in TILDA~\cite{romero-ortunoTransitionsFrailtyPhenotype2021}. The null increment here suggests that the functional limitation variables already in the BASE set capture the shared frailty signal, and grip strength adds little independent discrimination once those are present.

\textbf{Loneliness} (UCLA scale) produced no detectable effect (\gls{CI}: $[-0.009, 0.006]$). Chapter~\ref{ch:chapter2} highlighted the bidirectional relationship between social isolation and physical health decline in TILDA~\cite{santiniProtectivePropertiesActBelongCommit2017}. The null result does not contradict this relationship but suggests that in the context of predicting functional decline, loneliness does not carry discriminative information that is independent of the physical health and functional limitation variables already present.

\textbf{Depressive symptoms} produced a marginally positive delta ($0.713$ to $0.714$, \gls{CI}: $[-0.008, 0.010]$). Chapter~\ref{ch:chapter2} documented the bidirectional link between depression and functional decline~\cite{jacobFallsIncreaseRisk2023}. Again, the near-zero delta likely reflects that the BASE model's functional limitation predictors already capture much of the pathway through which depression relates to long-term illness.

\textbf{Illness limits activities (ph004)} produced an exact zero delta across all metrics, with \gls{CI}s of $[0.000, 0.000]$. This is a data availability issue: the variable existed only in waves where it was identical to an item already present in the BASE set, meaning no new information entered the model.
\begin{figure}[ht]
    \centering
    \includegraphics[width=0.9\textwidth]{add_on_fig.png}
    \caption{PLACEHOLDER: add a strong caption}\label{fig:add_on_fig}
\end{figure}

The consistent absence of significant add-on effects supports a key finding: for functional decline in this cohort, predictive signal is concentrated in the core health status and medication indicators. Additional lifestyle, socioeconomic, nutritional, cognitive, and social predictors contribute negligible independent discrimination once those are present. This finding reinforces the Track~A result that restricting to 20 features did not reduce performance relative to the full 261-feature set, and together they indicate that the \gls{TILDA} data for this prediction task is driven by accumulated health burden rather than by individual domain specific risk factors. % re verify the 261 number mentioned... where did you gt it from? is it whatthe model sees after all misisnigness etc is applied and hwta the model sees?

This has a direct practical implication. Chapter~\ref{ch:chapter1} outlined the research objective to identify actionable predictors for clinical intervention. The consistent ranking of medication burden, self-rated health, pain, and mobility difficulty across designs and models suggests these are the most informative screening targets for identifying older adults at risk of functional decline. All are routinely collected in clinical and community settings, supporting the feasibility of a lightweight screening tool using only a small number of self-reported items.% not sure if this is needed can drop..

\section{Error analysis}
\section{Summary of findings}